storage engr / systems engr / server engr
SOC engr (security operations center)
IAM engr (identity access management)
DevOps engr (powershell)
backup engr

cls // Clear screen

// Checks if a host is reachable, you can ping a website or IP address
ping www.dpwh.gov.ph // If you ping a web server, you will see the IP address
ping 8.8.8.8 // You can also ping a DNS server

ping -4 facebook.com // Forces an IPv4address
ipconfig // Shows the IP address, subnet mask, and default gateway of all network adapters
route print

telnet (IPv4address) (port no.)
nmap -v 45.60.157.243
nmap -sP 45.60.157.0/32

certutil -hashfile CERTIFICATEWEBSITE.ps1
